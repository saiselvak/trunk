export default class Sum {
    async Addition(a: number, b: number) {
        return a + b;
    }
    async Multiplication(a: number, b: number) {
        return a * b;
    }
    async Substraction(a: number, b: number) {
        return a - b;
    }
}
